version: "3.7"

services:
  mysql:
    image: mysql
    ports:
      - 3306:3306
    expose:
      - "3306"
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: books

  flask:
    image: esneidergrvc/flask-api:v1
    ports:
      - 3000:3000
    depends_on:
      - mysql

  angular:
    image: esneidergrvc/angular-ui:v1
    ports:
      - 4200:4200
