version: "3.7"

services:
  mysql:
    image: mysql:8.0.27
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: books
    ports:
      - 32000:3306

  flask:
    image: esneidergrvc/flask-api:v2
    ports:
      - 5000:5000
    links:
      - mysql
